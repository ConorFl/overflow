<%= form_for :answer, :url => { :action => "update" }, :html => {:method => "put"} do |f|%>
<%= f.label :content%><br>
<%= f.text_area :content, value: @answer.content %><br>
<%= f.submit :submit %>
<% end %>


